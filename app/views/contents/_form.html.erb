<%= form_for @content, url: (@content.id.nil? ? contents_path : content_path(id: @content)) do |f| %>

  <% if @content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@content.errors.count, "error") %> prohibited this content from being saved:</h2>

      <ul>
      <% @content.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :article_id %><br>
    <%=  f.collection_select :article_id, Article.all, :id, :title, include_blank: false %>

  </div>
  <div class="field">
    <%= f.label :doc %><br>
    <%= f.text_area :doc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
